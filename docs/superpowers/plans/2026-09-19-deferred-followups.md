# 後回しにした分の実装（捨て札の出どころ・戦記の拡充・任せるの範囲） Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. This document is a planning deliverable; it does not authorize production deploy by itself.

**Goal:** [捨て札と戦記](2026-09-16-public-record.md) の第2段と、[初期配置の同時化と反応窓](2026-09-16-concurrent-ready.md) の「後段」を、戦記そのものの読みやすさと合わせて片づける。戦記だけを読めば「誰が誰に何をして、判定がどうなったか」を追えるようにし、自分が捨てた札を見返せるようにし、伏せたまま捨てた札・従者防御・回収・再シャッフルを残す。長い対戦でも絞り込みとページ取得で追える。「任せる」を手番単位まで広げ、決着後は全員の手札と正体を開示する。

**Architecture:** 捨て札へ積む処理を1つの関数に集約し、札ごとに「誰が捨てたか」「表向きだったか」を持つ。これが自分の捨て札一覧と、伏せた札の記録の両方の土台になる。公開・非公開の線は変えない（その時点で表向きに場へ出たかで決める）。戦記の投影は既存の `logView` の許可リストのまま広げる。

**Spec:** 本計画。前提は [捨て札と戦記](2026-09-16-public-record.md)（第1段は main の `5b61a019`）、[初期配置の同時化と反応窓](2026-09-16-concurrent-ready.md)（PR A・B とも main）、[オンライン設計 §5・§6・§8](../specs/2026-09-07-online-game-design.md)、[G03](../../rules/second-edition/rulings.md)（優先権と同時入力）、[G11](../../rules/second-edition/rulings.md)（回収）。

**Worktree / ブランチ:** `/Users/chinju/git/madou-senki-record2`（ブランチ `deferred-followups`、main の `2cc32530` 上）。main への merge はユーザーが行う。

## 現状

捨て札:

- `PlayerView` は `discardCount` だけを送る。中身は誰にも送らない（第1段）
- `GameState.discard` は札IDの配列。誰が・表向きで捨てたかを持たない
- 捨て札へ積む処理は `packages/engine/src` の10ファイルに散らばる。`discardPhysical`（`discard.ts`）を通るのは10か所で、残りは `s.discard.push(...)` を直接呼ぶ
- 山札切れと大陸の夜明けで `s.discard=[]` になる（`lifecycle/advance.ts`）

戦記:

- 手番・カード使用・能力・判定・ダメージ・状態・距離・パス・「任せる」は記録済み（第1段と PR B）
- 未記録: 伏せたまま捨てた札、詠唱と従者の配置・並べ替え、表になった従者の防御と士気判定、回収、捨て札を山札へ戻す処理
- `PlayerView.logs` は毎回全件送る。bot の10席で公開イベント833件・公開ログ 68.2KB・スナップショット最大 79.2KB（`scripts/measure_public_record.ts`）。実戦は攻撃が多く、第1段の目安（公開ログ 100KB / スナップショット 200KB）を超える見込み
- 画面は全件スクロール・手番見出し・パスのまとめまで。絞り込みは無い

「任せる」:

- `GameState.standingPasses` は `{ rootEventId, actorIds }` の1件で、行動1つが終わるまで
- 介入・正体公開で消える（`transition.ts:139`）。窓が全部閉じても消える（同151）

決着後:

- `stableOutcome` で `outcome` が入るだけ。手札・捨て札・未公開の正体はそのまま隠れている

見送ったままの指摘:

- 同じ行動で「任せる」→解除→再度「任せる」で戦記が複数行になる（PR B の S001）
- `SET_CONDITIONAL_ABILITY` が内部名のまま画面に出る（PR B の P010。PR B の混入ではない既存の不具合）
- 初期配置バーのピル色とハイライトが、反応窓のバーと揃っていない（PR B の P008）
- 秘密漏れの E2E が40手ごとと最後だけ（第1段の R004）

## 公開範囲（変えない線）

第1段の原則をそのまま使う。「その時点で表向きに場へ出たものは全員の記録に残す。誰も見ていないものは残さない」。

| 場面 | 全員 | 本人だけ |
|---|---|---|
| 表向きで使った札、OPEN、表になった従者 | 札名 | 同じ |
| 伏せたまま捨てた札（手札調整・死亡時の手札・伏せた詠唱・裏向き従者・命中時の詠唱破棄） | 枚数 | 札名 |
| 詠唱・従者の配置・並べ替え | 事実と枚数 | 札名 |
| 回収（捨て札・解決領域から手札へ） | 札名（表向きで使われた札） | 同じ |
| 捨て札を山札へ戻す | 事実と枚数 | 同じ |
| 捨て札の中身 | 枚数だけ | 自分が捨てて今も残っている札 |
| 決着後 | 全員の正体・手札・捨て札・山札（下の Task 8） | 同じ |

自分の捨て札一覧に自分が表向きで使った札も入るが、それは戦記にも出ている公開情報なので新しい露出にはならない。

## 段取り

PR を6本に分ける。1が他の土台になるので最初。6は判断待ち。

| PR | 内容 | 依存 |
|---|---|---|
| 0 | 戦記の中身を読めるようにする（Task L1・L2・L3） | — |
| 1 | 捨て札の出どころと自分の捨て札一覧（Task 1・2） | — |
| 2 | 伏せた札・従者防御・回収・再シャッフルの記録（Task 3） | 1 |
| 3 | 戦記の絞り込みとページ取得（Task 4・5） | 2 |
| 4 | 決着後の全公開（Task 6） | 1 |
| 5 | 手番単位の「任せる」と、見送っていた軽微な指摘（Task 7・8） | — |

各タスクは失敗する試験を先に書き、エンジン → 投影 → 通信 → UI の順で通す。コミットはタスク単位（メッセージは各タスクの末尾）。工程の判定は `pnpm test` の全件（約2.5分）。既存試験の時間上限は延ばさない。

## Task L1: 誰が誰に何をしたかを戦記から読めるようにする

いまの戦記は、攻撃を「〈札名〉を攻撃に使いました（対象: ◯さん）」の1行で表す。札を使わない攻撃（従者攻撃・仮想従者・全軍突撃）は `cardInstanceId` が無く「カードを攻撃に使いました」になる。破壊された従者などは札名が文字列のままで、詳細を開けない。

- [x] 攻撃の宣言を「◯さんへ攻撃を宣言しました（〈札名〉）」の形にする。対象が複数ならすべて出す。踏み込み・間合い・防御・反撃も、相手が居る行動は相手を出す
- [x] 札を使わない攻撃を記録する: 従者攻撃は従者の札名（表になっているので公開）、仮想従者は能力名（`ABILITY_DECLARED` の公開規則に従う）、全軍突撃は使った札と従者
- [x] 戦記に出る札名・人物名・能力名はすべて詳細を開けるリンクにする（`CardLink` / `AbilityLink` を使う）。素のテキストで札名を出している箇所（`FOLLOWER_DESTROYED` など）を直す
- [x] 試験: 全イベント種類を1件ずつ描画し、札名・人物名・能力名が素のテキストで出ていないことを固定する。従者攻撃・仮想従者・全軍突撃の行
- コミット: `feat: name the target and the card in every public record line`

実装で分かったこと（2026-09-19）: 札を使わない攻撃のうち、記録が無かったのは仮想従者だけだった。従者攻撃は `follower-bundles.ts` が従者札を `CARD_PLAYED` で、全軍突撃は `all-army.ts` が全軍突撃せよと従者札の2枚を既に記録している。仮想従者のために `ATTACK_DECLARED`（能力名と対象。能力名は `ABILITY_DECLARED` と同じ公開規則）を足した。

## Task L2: 判定の見え方をそろえ、判定不要も記録する

いまは、正体を公開していない席の判定は、目標値も成否も他者に出ない（`view.ts` の `logView`、`rolls/advance.ts` の `projectRoll`）。卓上では出目も成否も全員が見るので、成否は公開に寄せる。目標値（精神力の値）は人物の能力値そのものなので伏せたままにする。

- [x] 裁定: G03 の隣に「判定の公開範囲」を足す。出目・合計・振り直し・成否・強制失敗は全員に見せ、目標値と修正の内訳は正体を公開している席だけに見せる。オンライン補完であることを明記する
- [x] `logView` と `projectRoll` の両方を同じ規則にする（戦記と判定パネルで食い違わせない）
- [x] 判定が要らなかった場合を記録する（`CHECK_SKIPPED`）。使用レベルが足りていて判定不要だった場合と、能力・カードで判定を免れた場合を区別する
  - 能力が理由なら、その席が正体を公開していれば能力名を出し、公開していなければ `concealed` にして「特殊能力で判定を免れました」とだけ出す（`recordAbility` と同じ扱い）
  - 判定が要るかどうかは卓上でも見えるので、事実そのものは全員に出す
- [x] 試験: 未公開の席の判定で、成否は出るが目標値は出ない。公開済みなら両方出る。判定不要・能力による免除の記録が公開・非公開それぞれで正しい。`public-record-privacy.test.ts` が通る
- [x] 画面: 判定の行を「〈目的〉で 3・5（合計8、目標値9・失敗）」のように読める形にする。目標値が見えない席では「（成功）」だけにする
- コミット: `feat: show every roll result and record skipped checks`

実装で分かったこと（2026-09-19）: `CHECK_SKIPPED` の理由は `level` / `ability` / `card` の3つにした。「能力・カードで免れた」を1つにまとめると、能力名を伏せた席で「特殊能力で判定を免れました」と出してしまい、カードの記述で判定不要だった場合と区別できなくなるため。記録は `attack.ts` の `continueAction` が効果Lv窓へ進む1か所（判定を1度も振らなかったときだけ）で行い、宣言窓が開き直しても二重に記録しない。

## Task L3: 表示順と読み方

既定は今のまま「古いものが上、新しいものが下」にする。攻撃 → 判定 → ダメージが起きた順に続けて読めること、既に自動追従と「最新へ」があることが理由。走り読み用に切り替えを足す。

- [x] 「新しい順 / 古い順」の切り替えを足す。新しい順では手番見出しも逆順にし、手番の中も新しい順にする。選択は同じ卓の再読み込みで保つ
- [x] 新しい順のときは自動追従を先頭方向にする（新着が上に積まれる）。「最新へ」は先頭へ戻る
- [x] `apps/web/test` に順序の試験（見出しと行、パスのまとめが逆順でも壊れない）
- コミット: `feat: let the reader flip the public record order`

実装で分かったこと（2026-09-19）: 並べ替えは `publicLogSections(view, order)` の最後で行う。パスのまとめは隣り合う記録を見るので、まとめ終えてから節と行を反転する。手番見出しは節の見出しなので位置は変わらず、節の並びと節内の行だけが逆になる。新しい順では番号が古い行から数えるよう `<ol reversed start=…>` にする。選択は `sessionStorage` に卓のパスごとに保存する。

## Task 1: 捨て札の出どころを1か所にまとめる

- [ ] `packages/engine/src/discard.ts` に `moveToDiscard(state, cardInstanceId, { ownerId, faceUp })` を作る。`GameState.discard` を `{ cardInstanceId, ownerId?, faceUp }[]` に替え、札IDだけが要る箇所のための `discardIds(state)` を出す
- [ ] `s.discard.push(...)` の直接呼び出し10か所を `moveToDiscard` に替える。各呼び出しで `faceUp` を決める根拠をコメントに1行残す（使用済みの行動札＝表、手札調整・死亡時の手札・伏せた詠唱・裏向き従者＝裏）
- [ ] `state.ts` の全札の突き合わせ（`return [...state.deck, ...state.discard, ...]`）、`reclaim.ts` の `s.discard.includes(...)`、`lifecycle/advance.ts` の再シャッフル（`s.discard=[]`）を新しい形に合わせる
- [ ] 保存済みの卓の読み替え: `apps/worker/src/rooms/room.ts` の `current()` で、`discard` が文字列の配列なら `{ cardInstanceId, faceUp: true }` に読み替える（PR A の `pending` の読み替えと同じ形。読み込みだけでは永続 revision を変えない）。表向き扱いにするのは、過去分の出どころが復元できないため。読み替えた卓では自分の捨て札一覧が空になることを受け入れる
- [ ] 試験: 10か所それぞれで `ownerId` と `faceUp` が入る、再シャッフルで空になる、回収で捨て札から抜ける、保存済みの文字列配列が読み替わる
- コミット: `refactor: record who discarded each card and whether it was face up`

## Task 2: 自分の捨て札一覧

- [ ] `view.ts`: `self.discardedCardInstanceIds`（自分が捨てて今も捨て札にある札、捨てた順）を足す。他者には `discardCount` のみ（現状維持）
- [ ] 試験（先に書く）: 他視点にこの配列が出ない、山札へ戻したら消える、回収で手札に戻したら消える、死亡で捨てた手札が本人には見える、bot の全試合で他者の札IDが混ざらない
- [ ] 画面: 見出しの「捨て札 N」をボタンにし、ダイアログで自分の捨て札を出す。新しい順と、同名をまとめた表示を切り替えられる。札名から既存の `CardDialog` を開ける。キーボードで開閉できる
- [ ] `apps/web/test` に表示試験
- コミット: `feat: let a seat review the cards it discarded`

## Task 3: 伏せた札・従者防御・回収・再シャッフルの記録

- [ ] `GameEvent.type` に足す: `CARDS_DISCARDED`（全員には枚数、本人には札名。Task 1 の `faceUp:false` の経路）、`CHANTED`、`FOLLOWERS_ARRANGED`、`FOLLOWER_DEFENDED`、`MORALE_CHECKED`、`CARD_RECLAIMED`、`DECK_RESHUFFLED`
- [ ] 記録は `moveToDiscard` と、従者防御・士気判定・回収の確定処理で行う。コマンドの入口ごとに書かない
- [ ] `logView` の許可リストに種類ごとの出してよいフィールドを足す。伏せた札は全員向けに枚数だけ
- [ ] 試験: 手札調整で捨てた札が他者には枚数だけ・本人には札名、死亡時の手札、命中時の詠唱破棄、従者防御と士気判定の並び、回収した札名、大陸の夜明けと山札切れ、`public-record-privacy.test.ts` が通る
- [ ] 画面: 新しい種類の文言。本人向けの記録は「自分だけに見えています」と分かる形で混ぜる
- コミット: `feat: record face-down discards, follower defense, reclaims and reshuffles`

## Task 4: 戦記の絞り込み

- [ ] 画面: 全員 / 自分に関係する記録 / 参加者別。選択は同じ卓の再読み込みで保つ（`sessionStorage`）
- [ ] 「自分に関係する」は、自分が行為者、自分が対象、自分の札が動いた記録とする。判定基準を1か所に置き、試験で固定する
- [ ] 絞り込み中に新しい記録が来たときの追従は、第1段の規則（追従中は末尾に留まる、上へ戻ると「最新へ」）のまま
- [ ] 受け入れ条件（数値）: bot の4人対戦で測り、**攻撃1回あたりの実質行数**（宣言からその攻撃の結末の行までに描画される行数）を数える。PR 0 時点は13行で、うちパス行が7行（54%。2026-09-20 に bot 4人対戦の3手番で実測）。絞り込みを「自分に関係する記録」にしたとき、自分が関わる攻撃1回あたり**7行以下**にする。測り方と実測値をコミットに書く
- [ ] `apps/web/test` に試験
- コミット: `feat: filter the public record by seat and relevance`

## Task 5: 過去分のページ取得

- [ ] まず計測する。`scripts/measure_public_record.ts` を Task 3 の後に実行し、公開ログとスナップショットの大きさを記録する。攻撃の多い卓を模した種でも測る
- [ ] 目安（公開ログ 100KB / 1回のスナップショット 200KB）を下回るなら、この Task は実装せず計測値を残して閉じる。判断と数値をコミットに書く
- [ ] 超えるなら: `PlayerView.logs` を最新50件と `logStart`（先頭のイベントID）にし、`packages/protocol` に `LOG_PAGE { beforeId, limit }`（1回最大200件）の要求と応答を足す。`room.ts` は `viewFor` と同じ投影で返す。閲覧専用の接続でも取れる
- [ ] 試験: 他者の秘密札名がページ応答に出ない、`beforeId` の境界、上限件数、再接続後に続きから取れる、決着後も取れる
- [ ] 画面: 上端までスクロールしたら過去分を読み、読み込み中と先頭に達したことを示す
- コミット: `feat: page the public record over the room socket`

## Task 6: 決着後の全公開

- [ ] 裁定: オンライン設計 §8 の表に「決着後は全員の正体・手札・捨て札・山札を開示する」を足す。原文に規定は無く、感想戦のためのオンライン補完であることを明記する
- [ ] `view.ts`: `state.outcome` があるときだけ、全席の人物・手札・伏せた詠唱・裏向き従者と、捨て札・山札の中身を出す。それ以外は現状維持
- [ ] 進行中に漏れないことを試験で固定する（`outcome` が無い状態では従来どおり）
- [ ] 画面: 結果画面で全員の正体・手札・捨て札を見られるようにする。戦記も全件見られる（既存）
- [ ] bot の全試合で、決着の1手前までは秘密が出ないことを確かめる
- コミット: `feat: open every hand and identity once the game ends`

## Task 7: 手番単位の「任せる」

- [ ] G03 に追記: 「任せる」には行動単位と手番単位がある。手番単位はその手番が終わるまで有効で、解除条件（介入・正体公開）は行動単位と同じ。手番が変わったら必ず消える
- [ ] `standingPasses` を `{ scope: 'action'; rootEventId } | { scope: 'turn'; turnNumber }` にし、`actorIds` は据え置き。`transition.ts` の消去条件に手番の変化を足す
- [ ] `PASS_ACTION_THROUGH` に `scope` を足す（省略時 `action`）。protocol の検証試験も足す
- [ ] 試験: 手番単位で押すと次の攻撃でも自動パスになる、介入で消えて聞き直される、手番が変わると消える、行動単位と同時に押されたときの優先、bot は使わない
- [ ] 画面: 「この行動は任せる」の隣に「この手番は任せる」。注記に「この手番のあいだ、割り込みの機会は流れます。誰かが動いたら聞き直します」と、回収の回答も含むことを書く。任せている範囲を状態行に出す
- コミット: `feat: let a seat pass through a whole turn`

## Task 8: 見送っていた指摘

- [ ] S001: 同じ行動で「任せる」→解除→再度「任せる」を繰り返しても、戦記は最後の状態を1行で表す
- [ ] P010: `SET_CONDITIONAL_ABILITY` に日本語名を足す。`choiceLabels` に無い内部名が画面へ出ていないか、全コマンドを走査する試験を足す
- [ ] P008: 初期配置バーのピルとハイライトを、反応窓のバーと同じ規則にそろえる（どちらかへ寄せる）
- [ ] R004: 秘密漏れの E2E を、40手ごとではなく全フレームの検査にする（受信のたびに検査する形）。時間が伸びる場合は席数を減らして全フレーム検査にする
- コミット: `fix: finish the deferred review findings`

## 入れないもの

- **割り込みの予約**（2026-09-16-concurrent-ready.md の後段）: 自分の順番が来る前にカードを出す意思を預けておく仕組み。ユーザー判断で今回は入れない（2026-09-19）。窓ごとにパスを選べば全ての境界で判断できる状態は保たれている
- 時間切れでの自動パス、手札に応じた自動パス（オンライン設計 §5 で不採用のまま）
- 初期配置での、置いた従者どうしの並べ替え（原文 8 は手番の行動としてだけ認めている）

## 受け入れ条件

- 戦記だけを読んで、誰が誰に何を使い、判定がどうなり、いくつダメージが入ったかを追える
- 戦記に出る札名・能力名・人物名から、その詳細を開ける
- 判定の出目と成否は全員に見え、目標値は正体を公開している席だけに見える。判定が不要だったこと、能力で免れたことも残る
- 自分が捨てた札を、いつでも一覧で確認できる。他者には枚数だけが見える
- 伏せたまま捨てた札は、全員には枚数、本人には札名で戦記に残る
- 従者防御・士気判定・回収・再シャッフルが戦記に残る
- 長い対戦でも、戦記を絞り込んで追える。送信量が第1段の目安を超えない（超える場合はページ取得が入っている）
- 決着後は全員の正体・手札・捨て札を見返せる。進行中は従来どおり隠れている
- 「任せる」を手番単位でも押せる。どちらの範囲かが押す前に分かる
- 秘密情報（進行中の手札・伏せた札・未公開の正体）が、画面・戦記・通信のどこにも出ない

## リスク

- **捨て札の形の変更が広い**: 10か所の呼び出しと、保存済みの卓の読み替えが要る。読み替えた卓は過去の出どころを復元できないので、一覧が空になる。段取りを1本目の PR に閉じ、`pnpm test` の全件で確かめる
- **記録漏れ**: 札の移動経路が多い。`moveToDiscard` に集約し、bot の全試合試験で「捨て札が増えたのに記録がない」を検出する
- **秘密漏れ**: 本人向けの記録と全員向けの記録を取り違えると漏れる。種類ごとに許可フィールドを試験で固定する
- **決着後の開示**: `outcome` の判定が途中で立つ経路があると、進行中に漏れる。`outcome` が無い状態を試験で固定する
- **手番単位の「任せる」で取り逃す**: 範囲が広いぶん後悔が増える。注記を明示し、解除をいつでも押せるようにする。行動単位を既定の並び順で先に置く
- **戦記の送信量**: Task L2・3 で記録が増える。Task 5 の計測で判断する
- **成否を公開に寄せる影響**: 目標値は伏せたままでも、成否が続けば精神力の範囲を絞れる。卓上と同じ情報量であり、許容する。目標値そのものと修正の内訳は出さない
