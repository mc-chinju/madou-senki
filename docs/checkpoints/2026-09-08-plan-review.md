# 2026-09-08 残工程計画・独立レビュー

対象: `docs/superpowers/plans/2026-09-08-completion-plan.md`、同日実装チェックポイント、`docs/implementation-progress.md`。原計画・設計、C15、Task7y報告、監査JSON、関連Engine/Worker/UI/カタログの現行ソースを読み合わせた。ランタイムの変更・復元、Git操作、ネットワーク操作、テスト実行は行っていない。97件中95成功・2失敗と154パス中9差分は今回の監査記録を参照した値であり、レビューによる再実行結果ではない。

## 判定

**承認（2指摘の修正を再読して確認済み）。** M0〜M4への組み直しとR0/R1から再開する方針は妥当。R2で全条項を監査してからR4〜R6の小計画を作る構成に、未監査の将来コマンドを今から固定する必要はない。未解決の必須指摘はない。計画整理の成果の承認であり、Task7yや完成版の実装受け入れを意味しない。

## 指摘と修正確認

1. **P2 — 現行HEADと再開判断の説明が矛盾する。** `docs/implementation-progress.md` の「Execution decisions」先頭（レビュー時62行）は、現在も「there is no committed HEAD to branch/worktree from」と説明する。冒頭の現行証拠と監査JSONはHEAD `8c65d10` を記録している。次の実装担当が旧制約を現状と取り違えるため、unborn mainは過去の判断と明記し、現在の履歴・作業ファイルを保持する方針へ更新する。`.git`の権限制約とHEADの有無は別に説明する。

2. **P2 — M0に至るEngineの既存回帰ゲートを明示する。** 改訂計画R0（レビュー時46〜60行）は3スイート97件を明示し、追加で数値プレビュー経路を検査する。一方、差分には `attack-properties.ts`、`declaration-candidates.ts`、`combat/legality.ts`、`combat/follower-bundles.ts` も含まれる。R1末尾（84〜90行）は全Web/Worker・型・ブラウザでM0としており、既存の宣言・攻撃属性・従者経路のEngine回帰を現在のソースへ対応付ける明示条件が不足する。旧Task7y報告の最終340件コマンドをR0の回帰検査として明記するか、差分ごとの影響を説明して必要な既存Engine/Protocolスイートを選び、全件成功とソースハッシュを保存する条件を追加する。旧成功数の転用や、修正のたびの無条件な全件再実行を求める指摘ではない。

再確認結果: 指摘1は、unborn HEADを過去と明記し、現在の `8c65d10` とGitメタデータ書込制約を分けた記述へ修正済み。指摘2は、現在の差分解消後に実行する9スイートの関連EngineゲートがR0へ追加され、97件だけの成功ではM0へ進めないこと、追加変更の回帰検査と選定理由の記録が明記された。両指摘をクローズする。

追加の引継ぎ確認: `.superpowers/` はGit管理外の補助履歴で、監査JSON・C15・現在の実装と固定入力テストを引継ぎの基礎にする説明を確認した。正式開始や実行テストが一時的な補助履歴へ依存する計画ではない。

## 確認できた点

- 25件は旧残件からの照合候補であり、未実装の確定件数ではない。Task7yの8件は別途受け入れ待ちで、ウーノス・ヴァンミールは既存ライフサイクルとの照合を先に行う。
- Task7yは実装者のエンジン報告と、独立レビュー・画面・DO・ブラウザの受け入れを区別している。Workerの現行試験は主に13fixtureの220枚保存と全設定OFFを調べるもので、R1がON・取消・更新・再送・復帰を残す説明は正しい。
- カタログは現行ソースでpendingを設定し、通常STARTは `assertPlayableCatalog` に失敗すると `RULESET_NOT_READY` を返す。R2だけで開始ゲートを緩めず、全条項受け入れ後のR7へ接続する順序は妥当。
- 旧保存版の一括復元は禁止され、現在差分の原因を断定していない。公開回復窓・一時不在・死亡前後の詳細をC15へ同期する作業も残している。
- 全220枚・26人物・110能力の条項、別用途・回収、原典32例、到達可能な状態列の保存則・公開範囲・再開同値が完成条件に含まれる。
- 限定シナリオの対人操作確認と正式8人通し対戦を自動操作から区別する。参加者待ちで独立作業を止めず、人間の確認自体も省略しない。
- Cloudflare実配備、休止復帰・ACK喪失・D1障害・更新・10人×10卓、素材条件、旧版卓保持が公開ゲートとして残る。今回の作業が計画整理だけであることも維持されている。

このレビューは残工程計画の整合性確認であり、全カードの正当性監査やTask7y実装の独立受け入れレビューを代替しない。
