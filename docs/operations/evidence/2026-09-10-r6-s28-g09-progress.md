# R6 S28のG09対応後の依存状態

現manifestに宣言されたS28の依存を機械的にたどり、8行すべてimplementedであることを記録した。以前pendingだったG09の明示選択/不使用秘匿2行にhandler・具体試験を接続した結果による。各辺・状態・remainingとledgerファイルhashは同名JSON。

これは宣言済みグラフの状態であり、出典全条件の網羅・独立レビュー・verified/acceptedの成立を意味しない。S28のdamage4条件を別のdamage3試験へ置き換えていない。R6未完チェックは閉じない。

次の候補を同じ宣言済み依存から抽出すると、S25がチャムの贈与3条項（任意の残手札1枚、使用済みsource除外、物理贈与との共存/二重カード禁止）をpendingとして参照していた。G09で既存のEngine/DO/browser経路を実行済みのため、出典と具体宣言をこの3条項へ限定して対応付ける。
