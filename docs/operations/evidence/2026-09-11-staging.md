# staging 実測（2026-09-14）

アカウントID・認証トークン・Cookieは書かない。料金の推定はしない。

## 対象

- origin: `https://madou-senki-staging.catalgorithm.workers.dev`
- Worker: `madou-senki-staging`
- D1: `madou-senki-staging`（UUID は `apps/worker/wrangler.jsonc` の `env.staging`）
- 初回配備 Version ID: `c2b793b1-436a-4d67-96db-cd5ea98eba15`
- 実施日: 2026-09-14

## リモート smoke

`PLAYWRIGHT_BASE_URL=<origin> pnpm exec playwright test -c playwright.remote.config.ts tests/remote/smoke.spec.ts`

- 2 passed
- 招待4人・準備・START・再読込で手札 region が見える
- `/__test` POST は成功しない（405）
- ACK 喪失後の再読込で同一 command が再送され、同じ ACK が返る

## DO 休止復帰

`REMOTE_HIBERNATE=1` で `tests/remote/hibernate.spec.ts` を実行。待ち時間は既定の 15 分。

- 1 passed（15.3m）
- 招待限定の待合（1人・準備完了）
- ブラウザを閉じ、同一 Cookie で `/rooms/:id` を開き直す
- 「準備を取り消す」が表示され、`GET /api/rooms/:id/snapshot` の revision が待ち前後で一致

開始後の手札でも同じ待ちを実測した。4人 START のあと全ブラウザを閉じ、同一 Cookie で `/rooms/:id` を開き直す。

- 卓: `d1d38030-1e96-4e29-a516-ed498f10f637`
- 待ち: 900000ms（実測 15.4m、終了 0）
- 復帰後も手札 region が見える
- snapshot revision は待ち前後とも 10

## D1 投影失敗

対局中（4人 START 後、手札表示）にリモート D1 の `room_directory` を一時リネームし、復帰させた。

- 卓: `845d5371-b133-441f-b519-55de8a21795d`
- 障害直前の snapshot revision: 10
- 一覧 `GET /api/rooms`: 障害中 500、テーブル復帰後 200
- 障害中の `GET /api/rooms/:id/snapshot`: 200、revision 10
- 障害中に別席を再読込しても手札 region は残る
- 障害中の閉卓同意で DO は revision 11 へ進む（試合正本は D1 に依存しない）
- テーブル復帰後、D1 の当該卓行は revision 11（outbox 再試行。backoff 上限 60s）
- 一覧は空でも 200 に戻る（招待卓は listing を持たない）

## 再配備中の試合維持

同じ卓を残したまま `wrangler deploy --env staging` を再実行。

- 配備前 revision: 11
- 配備後に別席を再読込しても手札 region は残る
- 配備後 snapshot revision: 11（不変）
- 配備 Version ID: `cd2d1a08-0d89-40b5-9b83-eeb13b489142`

## 負荷（10卓×10人）

招待リンクで 10 卓×10 席を作り、各席 200 手まで Bot を進めた。committed の `scripts/load_test.ts` は private 卓への invite なし join が 403 になるため、同等の招待発行と `STALE_REVISION` 再送を付けた実行で測った。所要 139s。

- 結果: [2026-09-11-staging-load.json](2026-09-11-staging-load.json)
- samples: 2110（READY 100 + START 10 + 進行 2000）
- failures: 0、disconnects: 0、failureRate: 0
- ACK 往復 ms: p50 50 / p95 67 / p99 107
- Cloudflare ダッシュボードの Requests / DO duration は、観測 MCP が未認証で CLI からもアカウントを出さずに数値を取れなかった。料金の推定はしない。
