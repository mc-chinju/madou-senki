# S25 チャムの残手札贈与3条項

対象は c2-p01-r2c2-ab05 の optional-one-hand-card-gift、residual-hand-excludes-used-source、coexists-physical-gift-no-double-card。原文の任意1枚手渡しとC11の死亡予定窓/使用中札除外/物理贈与との共存を、chamGiftOption・transitionChamGift・resolveChamGiftへ対応付けた。G09の2依存はimplemented。

既存Engineの併用ループをliteral it.eachへ変更し、能力先/物理先の具体parameterを登録可能にした。物理先では実使用済みの贈与札が捨て札にあり、チャム候補に含まれず、能力で偽装指定しても拒否するassertを追加した。両順序とも、すでに渡した札の再指定拒否と異なる札2枚の移動を確認する。

- Engine全10ケース成功。その後追加した使用済み札assertを含む両順序2ケースも成功。任意使用/不使用/取消、残手札、同時死亡対象除外、秘匿を含む。初期ダメージや配置は試験用前提で、正式STARTからの全履歴とは扱わない。
- DO4件: GOOD/EVILの物理贈与×能力先/物理先。各操作の休止復帰・ACK再送・保存状態一致、220枚一意と私的手札移動を確認。
- Browser4件: 同じ4組合せの実選択と再読込、使用札の候補除外、残手札の最終処分まで確認。初回grep先頭固定では対象0だったため修正して再実行した。成功は-final.logによる。
- 全型検査成功後、最後のEngine assertion変更にrootのtsc --noEmitを追加実行して成功。engine packageにはtypecheck scriptがなかったため、そのコマンドは型成功の証拠に含めない。

本体変更なし。18具体参照を接続し、3行をimplementedへ更新した。変更した旧動的宣言の既存台帳参照は0件だった。過去の実行証跡は変更しない。現候補の実行受領・独立レビュー・acceptedは未成立のまま。参照IDとログは同名JSON。
