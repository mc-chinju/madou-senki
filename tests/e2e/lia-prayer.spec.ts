import {expect,test} from '@playwright/test';
import {observe,passUntil,tableFixture} from './helpers.js';
for(const scenario of ['lia-prayer','lia-prayer-second'] as const)test(`${scenario} shows dedicated reservation and returns only after its actual event across reloads`,async({browser,request})=>{
 const table=await tableFixture(browser,request,scenario);try{const views=await observe(table),a=table.sessions[0]!.id,b=table.sessions[1]!.id,page=table.pages[1]!,prayer='a2-p05-r2c3';await page.reload();expect(views.get(b)!.game!.self.hand.filter(id=>id===prayer)).toHaveLength(1);await page.getByRole('combobox',{name:'割り込み効果',exact:true}).selectOption('effect-plus');await page.getByRole('checkbox',{name:'専用技として使う',exact:true}).check();await page.getByRole('combobox',{name:'使うカード',exact:true}).selectOption(prayer);const rev=views.get(a)!.revision;await page.getByRole('button',{name:'割り込みを使う',exact:true}).click();await expect.poll(()=>views.get(a)?.revision).toBeGreaterThan(rev);await page.reload();await passUntil(table,views,()=>views.get(b)!.game!.reservedCards.includes(prayer),300);await page.reload();expect(views.get(b)!.game!.self.hand).not.toContain(prayer);await passUntil(table,views,g=>g.activeWindow?.kind==='effect-level'&&g.activeWindow.pendingActorId===b,300);await expect(page.locator(`select option[value="${prayer}"]`)).toHaveCount(0);await passUntil(table,views,g=>!g.activeWindow,300);await page.reload();expect(views.get(b)!.game!.self.hand.filter(id=>id===prayer)).toHaveLength(1);expect(views.get(b)!.game!.reservedCards).toEqual([]);}finally{await table.close();}
});
