import {expect,test,type Locator} from '@playwright/test';
import {deathKnightPhysicalMode,deathKnightPhysicalScenarios} from '../../apps/worker/test/fixtures/death-knight-physical-scenarios.js';
import {observe,windowPassButtonName,tableFixture} from './helpers.js';
for(const scenario of deathKnightPhysicalScenarios)test(`${scenario} actual placement defense or granted DeathKnight attack survives reload`,async({browser,request})=>{
 const table=await tableFixture(browser,request,scenario),m=deathKnightPhysicalMode(scenario),errors:string[]=[];for(const p of table.pages)p.on('websocket',socket=>socket.on('framereceived',frame=>{const message=JSON.parse(String(frame.payload));if(message.type==='error')errors.push(message.code);}));
 try{const views=await observe(table),a=table.sessions[0]!.id,b=table.sessions[1]!.id,c=table.sessions[2]!.id,page=table.pages[0]!,bp=table.pages[1]!;async function click(button:Locator){const rev=views.get(a)!.revision;await button.click();await expect.poll(()=>{expect(errors).toEqual([]);return views.get(a)?.revision;}).toBeGreaterThan(rev);}async function settle(stop?:string){for(let n=0;n<300;n++){const current=views.get(a)!,g=current.game!,w=g.activeWindow;if(!w||w.kind===stop)return;if(m.waive&&w.kind==='normal-defense'&&w.pendingActorId===a){await page.getByRole('checkbox',{name:'デス・ナイトの専用効果を使う',exact:true}).check();await click(page.getByRole('button',{name:'従者で受ける',exact:true}));continue;}const actor=w.pendingActorId!;await expect.poll(()=>views.get(actor)?.revision).toBe(current.revision);await table.pages[table.sessions.findIndex(p=>p.id===actor)]!.getByRole('button',{name:windowPassButtonName}).click();await expect.poll(()=>{expect(errors).toEqual([]);return views.get(a)?.revision;}).toBeGreaterThan(current.revision);}throw Error('DEATH_KNIGHT_UI_LIMIT');}
 if(m.initial){await page.getByRole('region',{name:'自分の手札',exact:true}).getByRole('button',{name:m.name,exact:true}).click();await click(page.getByRole('button',{name:'従者を置く',exact:true}));await page.reload();}for(let i=0;i<4;i++)await click(table.pages[i]!.getByRole('button',{name:'従者を置かず進む',exact:true}));await click(page.getByRole('button',{name:'手番を始める',exact:true}));await click(page.getByRole('button',{name:'カードを引かない',exact:true}));if(!m.defense){if(m.dedicated){const panel=page.getByRole('region',{name:'従者による攻撃',exact:true});await panel.getByRole('combobox',{name:'攻撃に使う従者',exact:true}).selectOption(m.card);await panel.getByRole('checkbox',{name:'専用の攻撃として使う',exact:true}).check();await click(panel.getByRole('button',{name:'従者で攻撃する',exact:true}));}else if(m.army){const panel=page.getByRole('region',{name:'全軍突撃せよ',exact:true});await panel.getByRole('combobox',{name:'突撃に使う従者',exact:true}).selectOption(m.card);await click(panel.getByRole('button',{name:'2枚を使って全軍突撃する',exact:true}));}}else{if(!m.initial){const panel=page.getByRole('region',{name:'従者の配置',exact:true});await panel.getByRole('combobox',{name:'従者を加える',exact:true}).selectOption(m.card);await click(panel.getByRole('button',{name:'この順番で確定',exact:true}));await page.reload();}expect(views.get(a)!.game!.self.followers).toContainEqual(expect.objectContaining({cardInstanceId:m.card,revealed:false}));for(let n=0;n<150;n++){const g=views.get(a)!.game!;if(g.activeWindow){await settle();continue;}if(g.phase==='action'&&g.seatOrder[g.turnSeat]===b)break;const label=g.phase==='withdrawal'?'離脱しない':g.phase==='hand-adjustment'?'選んだ0枚を捨てて手番を終える':g.phase==='action'?'行動を終える':g.phase==='draw'?'カードを引かない':'手番を始める';await click(table.pages[g.turnSeat]!.getByRole('button',{name:label,exact:true}));}if(!m.white&&!m.destroy){const panel=bp.getByRole('region',{name:'複数従者の攻撃'});await panel.getByRole('combobox',{name:'使う能力',exact:true}).selectOption('c2-p06-r1c2-ab04');await panel.getByRole('combobox',{name:'追加する従者',exact:true}).selectOption('a2-p21-r2c3');await panel.getByRole('button',{name:'攻撃に加える',exact:true}).click();await panel.getByRole('radio',{name:table.sessions[0]!.name,exact:true}).check();await click(panel.getByRole('button',{name:'選んだ従者で攻撃する',exact:true}));}else{const index=views.get(b)!.game!.self.hand.indexOf(m.white?'a2-p14-r1c3':'a2-p13-r1c2');await bp.getByRole('region',{name:'自分の手札',exact:true}).getByRole('article').nth(index).getByRole('button').first().click();await bp.getByRole('checkbox',{name:'専用技として使う',exact:true}).setChecked(true);await bp.getByRole('region',{name:'参加者の公開状態'}).getByRole('article').filter({has:bp.getByRole('heading',{name:table.sessions[0]!.name,exact:true})}).getByRole('checkbox',{name:'対象に選ぶ'}).check();await click(bp.getByRole('button',{name:'攻撃を確認して実行',exact:true}));}}if(!m.defense){await settle('normal-defense');const panel=bp.getByRole('complementary',{name:'現在の判断'}),pay=panel.getByRole('button',{name:'間合いを使う',exact:true});await panel.getByRole('combobox',{name:'使うカード',exact:true}).selectOption('a2-p07-r1c2');await expect(pay).toBeDisabled();const bundle=panel.getByRole('group',{name:'一緒に支払う間合い'});await bundle.getByRole('checkbox').check();await expect(pay).toBeEnabled();await bp.reload();await expect(pay).toBeDisabled();await panel.getByRole('combobox',{name:'使うカード',exact:true}).selectOption('a2-p07-r1c2');await bundle.getByRole('checkbox').check();await click(pay);expect(views.get(b)!.game!.self.hand).not.toContain('a2-p07-r1c2');expect(views.get(b)!.game!.self.hand).not.toContain('a2-p07-r1c3');await bp.reload();}await page.reload();await settle();await page.reload();const done=views.get(a)!.game!,alive=m.defense&&!m.white&&!m.destroy&&!m.fail&&m.attackLevel<=7;expect([done.players[a]!.damage,done.players[b]!.damage,done.players[c]!.damage,done.players[table.sessions[3]!.id]!.damage]).toEqual(!m.defense?[0,0,8,8]:[m.white?10:m.destroy?0:m.fail||m.attackLevel>7?18:0,0,0,0]);expect(done.self.followers.map(f=>f.cardInstanceId)).toEqual(alive?[m.card]:[]);expect(done.self.hand).not.toContain(m.card);expect(done.discard.filter(id=>id===m.card)).toHaveLength(alive?0:1);if(m.army)expect(done.discard.filter(id=>id==='a2-p05-r2c2')).toHaveLength(1);expect(done.recentRolls.filter(r=>r.purpose==='follower-morale')).toHaveLength(m.white||m.destroy||m.waive||m.dedicated?0:1);if(!m.defense)for(const id of ['a2-p07-r1c2','a2-p07-r1c3'])expect(done.discard.filter(x=>x===id)).toHaveLength(1);expect(errors).toEqual([]);
 }finally{await table.close();}
});
