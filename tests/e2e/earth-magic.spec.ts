import {expect,test,type Locator} from '@playwright/test';
import {observe,passUntil,tableFixture} from './helpers.js';
import {earthMagicMode} from '../../apps/worker/test/fixtures/earth-magic-scenarios.js';
for(const scenario of ['earth-spear-ordinary','earth-spear-dedicated','earth-stream-ordinary','earth-stream-dedicated'] as const)test(`${scenario} actual dedicated choice and printed maai cost persist across reload`,async({browser,request})=>{
 const table=await tableFixture(browser,request,scenario);
 try{const views=await observe(table),a=table.sessions[0]!.id,b=table.sessions[1]!.id,c=table.sessions[2]!.id,page=table.pages[0]!,bp=table.pages[1]!,m=earthMagicMode(scenario);async function click(button:Locator){const rev=views.get(a)!.revision;await button.click();await expect.poll(()=>views.get(a)?.revision).toBeGreaterThan(rev);}await page.getByRole('region',{name:'自分の手札',exact:true}).getByRole('button',{name:m.name,exact:true}).click();await page.getByRole('checkbox',{name:'専用技として使う',exact:true}).setChecked(m.dedicated);for(const index of m.dedicated?[1,2]:[1])await page.getByRole('region',{name:'参加者の公開状態'}).getByRole('article').filter({has:page.getByRole('heading',{name:table.sessions[index]!.name,exact:true})}).getByRole('checkbox',{name:'対象に選ぶ'}).check();await click(page.getByRole('button',{name:'攻撃を確認して実行',exact:true}));await page.reload();await passUntil(table,views,g=>g.activeWindow?.kind==='normal-defense');await bp.reload();expect(views.get(a)!.game!.currentAttack!.technique).toMatchObject({effectLevel:m.effect,damage:m.damage});for(const maai of ['a2-p07-r1c1','a2-p07-r1c2'].slice(0,m.maai)){await bp.getByRole('combobox',{name:'使うカード',exact:true}).selectOption(maai);await click(bp.getByRole('button',{name:'間合いを使う',exact:true}));await passUntil(table,views,g=>g.activeWindow?.kind!=='reclaim');await bp.reload();}await passUntil(table,views,g=>!g.activeWindow,300);await page.reload();const done=views.get(a)!.game!;expect([done.players[b]!.damage,done.players[c]!.damage]).toEqual([0,m.dedicated?m.damage:0]);expect(done.self.hand).not.toContain(m.card);
 }finally{await table.close();}
});
