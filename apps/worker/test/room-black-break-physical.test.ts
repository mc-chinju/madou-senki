import {reset} from 'cloudflare:test';
import {afterEach,expect,it,vi} from 'vitest';
import {activeWindowRef,allCardInstanceIds,viewFor,type GameCommand,type GameState} from '@madou/engine';
import {openTestRoom} from './fixtures/recovery-room.js';
import {blackBreakScenarios,blackBreakCard as CARD,blackBreakMode} from './fixtures/black-break-physical-scenarios.js';
afterEach(async()=>{vi.restoreAllMocks();await reset();});
it.each(blackBreakScenarios)('%s physical near sword white destruction and ordered followers persist through every restart replay',async scenario=>{
 vi.spyOn(crypto,'getRandomValues').mockImplementation(<T extends ArrayBufferView|null>(array:T):T=>{if(array instanceof Uint32Array)array.fill(2);return array;});const room=await openTestRoom(scenario),m=blackBreakMode(scenario);let seq=0;const state=async()=>(await room.stored()).state.game!;
 async function send(actorId:string,command:GameCommand){const stored=await room.stored(),envelope={protocolVersion:1 as const,commandId:`black-break-${seq++}`,expectedRevision:stored.revision,...activeWindowRef(stored.state.game!),command},ack=await room.command(actorId,envelope);expect(ack,JSON.stringify({command,ack})).toMatchObject({type:'ack'});const saved=await room.stored(),ids=allCardInstanceIds(saved.state.game!);expect(ids).toHaveLength(220);expect(new Set(ids).size).toBe(220);await room.restart();expect(await room.command(actorId,envelope)).toEqual(ack);expect(await room.stored()).toEqual(saved);}
 async function reject(actorId:string,command:GameCommand){const stored=await room.stored(),envelope={protocolVersion:1 as const,commandId:`black-break-invalid-${seq++}`,expectedRevision:stored.revision,...activeWindowRef(stored.state.game!),command},error=await room.command(actorId,envelope);expect(error).toMatchObject({type:'error'});expect((await room.stored()).state.game).toEqual(stored.state.game);await room.restart();expect(await room.command(actorId,envelope)).toEqual(error);expect((await room.stored()).state.game).toEqual(stored.state.game);}
 async function until(done:(s:GameState)=>boolean){for(let n=0;n<600;n++){if(done(await state()))return;const w=(await state()).windows!.at(-1)!;await send(w.participants[w.cursor]!,{type:'PASS'});}throw Error('BLACK_BREAK_DO_LIMIT');}
 const initial=structuredClone(await state()),command:GameCommand={type:'ATTACK',cardInstanceId:CARD,targetIds:['B'],dedicated:m.dedicated};
 if(m.suffix==='stopped'||m.suffix==='wrong-owner'){await reject('A',command);expect((await state()).players.A!.hand).toContain(CARD);return;}if(m.suffix==='decline'){await send('A',{type:'PASS_ACTION'});expect((await state()).players.A!.hand).toContain(CARD);return;}
 if(m.suffix==='noncounter'){for(const dedicated of [false,true])await reject('A',{type:'PLAY_DEFENSE',cardInstanceId:CARD,dedicated});await until(s=>!s.windows?.length);expect((await state()).players.A!.damage).toBe(4);expect((await state()).players.A!.hand).toContain(CARD);return;}
 if(m.suffix==='far'){await reject('A',command);await send('A',{type:'APPROACH',cardInstanceId:'a2-p24-r1c3',targetId:'B'});await until(s=>!s.windows?.length);}if(m.suffix==='same-faction')await reject('A',{...command,targetIds:['C']});const before=await state();await send('A',command);expect((await state()).players.A!.hand).toEqual(before.players.A!.hand.filter(id=>id!==CARD));expect((await state()).deck).toEqual(before.deck);expect((await state()).resolution).toContain(CARD);
 if(m.suffix==='fate'){const id=Object.values((await state()).actions!).find(a=>a.cardInstanceId===CARD)!.id;await until(s=>s.windows!.at(-1)!.participants[s.windows!.at(-1)!.cursor]==='D');await send('D',{type:'PLAY_REACTION',cardInstanceId:'a2-p02-r2c3',mode:'cancel',targetActionId:id});await until(s=>!s.windows?.length);expect((await state()).players.B!.damage).toBe(0);expect((await state()).players.B!.followers).toEqual(initial.players.B!.followers);expect((await state()).discard.filter(id=>id===CARD)).toHaveLength(1);return;}
 await until(s=>s.windows?.at(-1)?.kind==='attack-abilities');expect(Object.values((await state()).groups!)[0]!.technique).toMatchObject({school:'warrior',range:'near',useLevel:4,effectLevel:m.dedicated?5:4,damage:m.dedicated?7:5,attributes:['戦','剣'],destroyFollowerAttributes:['白'],target:'one',chant:false,counter:false,noChecks:m.dedicated,followerIgnore:false});
 if(m.suffix==='maai'||m.suffix==='evade'){await until(s=>s.windows?.at(-1)?.kind==='normal-defense');expect(viewFor(await state(),'B').currentAttack!.defenseRestrictions).toMatchObject({maaiProhibited:false,evadeProhibited:false});await send('B',m.suffix==='maai'?{type:'PLAY_MAAI',cardInstanceId:'a2-p06-r1c3'}:{type:'PLAY_DEFENSE',cardInstanceId:'a2-p05-r3c1',dedicated:false});}
 await until(s=>!s.windows?.length);const final=await state(),avoided=['maai','evade'].includes(m.suffix),blocked=['nonwhite','wall-first'].includes(m.suffix),reduced=['lower','front-white','back-white'].includes(m.suffix);expect(final.players.B!.damage).toBe(avoided||blocked?0:reduced?5:m.dedicated?7:5);expect(final.players.C!.damage).toBe(0);expect(final.players.C!.followers).toEqual(initial.players.C!.followers);if(avoided)expect(final.players.B!.followers).toEqual(initial.players.B!.followers);else if(blocked){expect(final.players.B!.followers.map(f=>f.cardInstanceId)).toEqual(m.followers);expect(final.players.B!.followers[0]!.revealed).toBe(true);if(m.suffix==='wall-first')expect(final.players.B!.followers[1]!.revealed).toBe(false);}else{expect(final.players.B!.followers).toEqual([]);for(const id of m.followers)expect(final.discard).toContain(id);}expect(final.discard.filter(id=>id===CARD)).toHaveLength(1);if(m.suffix==='low')expect(final.rolls!.at(-1)).toMatchObject({purpose:'excess-level',threshold:6,total:6,success:true});else expect(final.rolls?.length??0).toBe(initial.rolls?.length??0);
},15000);
