import {reset} from 'cloudflare:test';
import {afterEach,expect,it,vi} from 'vitest';
import {activeWindowRef,allCardInstanceIds,type GameCommand} from '@madou/engine';
import {openTestRoom} from './fixtures/recovery-room.js';
import {whiteDragonMode} from './fixtures/white-dragon-scenarios.js';
afterEach(async()=>{vi.restoreAllMocks();await reset();});
it.each(['white-sword-ordinary','white-sword-first','white-sword-inherited','white-sword-full','white-slash-ordinary','white-slash-first','white-slash-inherited','white-slash-full'] as const)('%s saves the physical selected package and shared damage roll once across eviction and changed entropy',async(scenario)=>{
 let changed=false;vi.spyOn(crypto,'getRandomValues').mockImplementation(<T extends ArrayBufferView|null>(array:T):T=>{if(array instanceof Uint32Array)for(let i=0;i<array.length;i++)array[i]=changed?5:i%4+1;return array;});const room=await openTestRoom(scenario);let seq=0;const m=whiteDragonMode(scenario),multi=scenario.startsWith('white-slash')&&m.dedicated;
 async function send(actorId:string,command:GameCommand){const before=await room.stored(),envelope={protocolVersion:1 as const,commandId:`white-dragon-${seq++}`,expectedRevision:before.revision,...activeWindowRef(before.state.game!),command};const ack=await room.command(actorId,envelope);expect(ack).toMatchObject({type:'ack'});const saved=await room.stored(),ids=allCardInstanceIds(saved.state.game!);expect(ids).toHaveLength(220);expect(new Set(ids).size).toBe(220);if(saved.state.game!.rolls?.some(r=>r.purpose==='attack-damage'&&r.stage==='after-roll'))changed=true;await room.restart();expect(await room.command(actorId,envelope)).toEqual(ack);expect(await room.stored()).toEqual(saved);}
 await send('A',{type:'ATTACK',cardInstanceId:m.card,targetIds:multi?['B','C']:['B'],dedicated:m.dedicated,...(m.variant?{techniqueVariant:m.variant}:{})});const declared=(await room.stored()).state.game!,f=Object.values(declared.actions!).find(a=>a.cardInstanceId===m.card)!;expect(f.fromChant??false).toBe(m.chant);expect(f.technique).toMatchObject({useLevel:m.use,effectLevel:m.effect,chant:m.chant,noChecks:m.dedicated});for(let n=0;n<300;n++){const s=(await room.stored()).state.game!,w=s.windows?.at(-1);if(!w)break;await send(w.participants[w.cursor]!,{type:'PASS'});}const done=(await room.stored()).state.game!;expect([done.players.B!.damage,done.players.C!.damage,done.players.D!.damage]).toEqual([m.damage,multi?m.damage:0,0]);expect(done.players.A!.chants).toEqual([]);expect(done.discard.filter(id=>id===m.card)).toHaveLength(1);expect(done.players.A!.hand).not.toContain(m.card);if(multi&&m.variant==='lancelot-1'){const rolls=done.rolls!.filter(r=>r.purpose==='attack-damage');expect(rolls).toHaveLength(1);expect(rolls[0]).toMatchObject({formula:'4d6+1',faces:[2,3,4,5],total:15});expect(rolls[0]!.attempts).toHaveLength(1);}expect(done.phase).toBe('withdrawal');
});
